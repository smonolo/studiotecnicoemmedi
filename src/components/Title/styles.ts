import styled from "@emotion/styled";

export const StyledTitle = styled.div`
    font-size: 40px;
    font-weight: 700;
    color: #444450;
    text-align: center;

    @media (max-width: 900px) {
        font-size: 25px;
    }
`;